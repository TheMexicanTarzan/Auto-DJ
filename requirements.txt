# Audio I/O  (librosa is retained *only* for file loading and resampling)
# System dependency: ffmpeg (required for m4a/aac decoding)
#   sudo apt-get install ffmpeg   # Debian/Ubuntu
#   brew install ffmpeg            # macOS
librosa>=0.10.0
soundfile>=0.12.0
pydub>=0.25.0

# Beat tracking & downbeat detection
# madmom provides RNN-based beat/downbeat tracking (state of the art).
madmom>=0.16.1

# Key detection
# essentia provides the KeyExtractor algorithm for global key estimation.
# NOTE: If pip install fails, try:  conda install -c mtgdb essentia
# or use the pre-built wheels:  pip install essentia==2.1b6.dev1184
essentia>=2.1b6

# ML embeddings (CLAP model)
transformers>=4.30.0
torch>=2.0.0

# Graph & pathfinding
python-igraph>=0.11.0

# Visualization
plotly>=5.0.0

# Utilities
numpy>=1.24.0
