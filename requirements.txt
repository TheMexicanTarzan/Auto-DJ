# Audio I/O, beat tracking & key detection
# essentia provides MonoLoader (ffmpeg-backed audio I/O),
# RhythmExtractor2013 for BPM/beat detection, and KeyExtractor for
# global key estimation.
# System dependency: ffmpeg (required for m4a/aac decoding)
#   sudo apt-get install ffmpeg   # Debian/Ubuntu
#   brew install ffmpeg            # macOS
# NOTE: If pip install fails, try:  conda install -c mtgdb essentia
# or use the pre-built wheels:  pip install essentia==2.1b6.dev1184
essentia>=2.1b6

# ML embeddings (CLAP model)
transformers>=4.30.0
torch>=2.0.0

# Graph & pathfinding
python-igraph>=0.11.0

# Web server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Fast JSON serialization
orjson>=3.9.0

# Audio fingerprinting (Chromaprint)
# System dependency: libchromaprint-dev (Debian/Ubuntu) or chromaprint (brew)
#   sudo apt-get install libchromaprint-dev   # Debian/Ubuntu
#   brew install chromaprint                   # macOS
pyacoustid>=1.2.0

# Utilities
numpy>=1.24.0
